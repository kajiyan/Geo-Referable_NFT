# セキュリティ: 信頼できるパッケージのビルドスクリプトを許可
# pnpm 10.xではデフォルトでビルドスクリプトが無効化されるため、
# 必要なパッケージのみ明示的に許可リストから除外
ignoredBuiltDependencies=[]

# CSS @import "tailwindcss" の解決に必要
# Next.js devモードのCSSリゾルバーがモノレポルートから解決するため、ルートにhoist必須
public-hoist-pattern[]=tailwindcss
