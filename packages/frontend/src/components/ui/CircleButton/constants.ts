/**
 * CircleButton Component Constants
 *
 * Sizing for circular icon buttons with two size variants:
 * - default: ~58px diameter, 24px icon
 * - sm: ~40px diameter, 20px icon
 *
 * @see CircleButton.tsx for how these constants are consumed
 */

/** Icon sizes per variant */
export const ICON_SIZE = 24 as const
export const ICON_SIZE_SM = 20 as const

/** Spinner sizes per variant */
export const SPINNER_SIZE = 20 as const
export const SPINNER_SIZE_SM = 14 as const

/** Default button diameter (for reference) */
export const BUTTON_DIAMETER = 58 as const
